export function TaskDetails({ task, isOpenDetails }) {
    return <section className='task-details' style={{ right: `${isOpenDetails ? 10 : 20}vw` }}>
        {/* <h1>TaskDetails</h1> */}

    </section>
}